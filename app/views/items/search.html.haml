=render "header"
.search-main
 .search-main__left

 .search-main__right
  .search-main__right__title
   =@search
   の検索結果
  .search-main__right__item-box
  -@items.each do |item|
   -if item.item_images.present?
    = image_tag "#{item.item_images[0].image}", size: '220x200'
    = item.name
    = item.price

=render "footer"